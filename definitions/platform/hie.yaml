# MANUALLY POPULATED
platform_type: hie

scopes_supported:
  - launch/patient
  - fhirUser
  - openid
  - patient/*.read
grant_types_supported:
  - authorization_code
response_types_supported:
  - code
response_modes_supported:
  - query
code_challenge_methods_supported:
  - S256
confidential: true
cors_relay_required: false

documentation: |
  This is a placeholder platform type. Patients will need to do identity verification with an IAL2 provider and then 
  request medical records out of band.
client_headers:
  Accept: "application/json+fhir"
missing_op_patient_everything: true
client_supported_resources: []
